# React Vite Starter Project

Basic React app created with Vite.  
Default template removed and replaced with minimal App component content and styles.

Run with:

Install dependencies:
npm install

Start development server:
npm run dev
